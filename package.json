{
  "name": "mpt",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --parallel --stream -r run dev",
    "build": "pnpm --stream -r run build"
  },
  "devDependencies": {
    "@rsbuild/core": "1.1.8",
    "typescript": "5.7.2",
    "nx": "20.3.0",
    "@nx/rsbuild": "20.3.0"
  }
}